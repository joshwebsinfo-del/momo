version: '3.9'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      DEBUG: "True"
      SECRET_KEY: "replace-me"
      ALLOWED_HOSTS: "localhost,127.0.0.1"
    volumes:
      - .:/app
